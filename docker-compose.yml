version: '3.8'

services:
  medvibe-terminal:
    build: .
    container_name: medvibe_core
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - medvibe_storage:/app
    restart: unless-stopped

volumes:
  medvibe_storage:
    driver: local
